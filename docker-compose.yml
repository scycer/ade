services:
  ade:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ade
    ports:
      - "${PORT:-6969}:6969"
    volumes:
      # Mount ADE
      - ./:/app
    env_file:
      - .env
    restart: unless-stopped
