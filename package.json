{
  "name": "ade",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "bun run --cwd app dev",
    "build": "bun run --cwd app build",
    "test": "bun run --cwd app test",
    "validate": "bun run --cwd app validate",
    "rebuild": "bun ./tools/rebuild-container.ts",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
